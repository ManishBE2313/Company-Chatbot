# Web Framework & Server
fastapi==0.110.0
uvicorn==0.27.1
python-dotenv==1.0.1

# AI & Orchestration Framework
langchain==0.1.13
langchain-community==0.0.29
langchain-groq==0.1.2
langgraph==0.0.30
langchain-text-splitters==0.0.1

# Vector Database Client
qdrant-client>=1.9.0
langchain-qdrant>=0.1.4

# Local Embeddings Support
sentence-transformers==2.5.1

# Document ingestion
docling

# Add these to your existing requirements.txt
PyMuPDF==1.23.21      # For fast, reliable PDF text extraction (imported as 'fitz')
httpx==0.27.0         # For async HTTP requests (downloading CVs and sending webhooks)
PyPDF2                # For in-memory PDF extraction (used in batch_intake.py)

# Microsoft Graph API / SharePoint
# msal                  # Required for Entra ID authentication
requests              # Required for standard Graph API requests